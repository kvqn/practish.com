# Problem Statement

Oops! A file named `index.htlm` has a typo in its extension. Your task is to **rename** it correctly to `index.html`.

# Instructions

1. Rename the file `index.htlm` to `index.html`.
