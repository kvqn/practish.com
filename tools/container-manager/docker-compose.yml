services:
  practish-container-manager:
    container_name: practish-container-manager
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    networks:
      - easyshell
    restart: always
networks:
  easyshell:
    name: easyshell
    external: true
